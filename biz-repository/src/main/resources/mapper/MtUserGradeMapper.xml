<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.repository.cloud.plambiz.MtUserGradeMapper">
    <select id="getMerchantGradeList" resultType="model.repository.cloud.plambiz.MtUserGrade">
        select * from mt_user_grade t where t.STATUS != 'D'
        <if test="merchantId != null and merchantId > 0">
           and t.MERCHANT_ID = #{merchantId}
        </if>
        <if test="status != null and status != ''">
           and t.STATUS = #{status}
        </if>
    </select>
</mapper>
