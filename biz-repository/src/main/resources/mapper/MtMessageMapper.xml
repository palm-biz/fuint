<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.repository.cloud.plambiz.MtMessageMapper">
    <select id="findNewMessage" resultType="model.repository.cloud.plambiz.MtMessage">
        select * from mt_message o where o.USER_ID = #{userId} and o.STATUS = 'A' and o.TYPE = #{type} and o.IS_READ = 'N' order by o.ID desc
    </select>
    <select id="findNeedSendMessage" resultType="model.repository.cloud.plambiz.MtMessage">
        select * from mt_message t where t.TYPE = #{type} and t.IS_SEND = 'N' and TIMESTAMPDIFF(HOUR, CREATE_TIME, NOW())  <![CDATA[ <= ]]> 1 order by t.ID desc
    </select>
</mapper>
